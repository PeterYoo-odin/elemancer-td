node_modules
dist
.DS_Store
*.log
.vercel
.env
.env.*
# Balance harness machine artifact (regenerated by `npm run balance`, uploaded in CI).
# The human-readable BALANCE_REPORT.md IS committed as the documented deliverable.
balance-report.json
